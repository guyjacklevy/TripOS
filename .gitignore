.DS_Store
.vercel

# internal agent-coordination workspace — never publish (repo is public)
_agents/

# local tooling config
.claude/

# secrets must never enter the repo (real home: ~/Documents/prevoya-secrets/)
client_secret_*.json
*credentials*.json
.env
.env.*
